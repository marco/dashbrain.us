import React from 'react';
import { EventStudentJoin } from '../../lib/events';
import { Room } from '../../lib/rooms';

let WelcomeEvent: React.FC = (props) => {
  return <div>Welcome to Dashbrain!</div>;
};

export default WelcomeEvent;
